<%= form_for(@ip_name) do |f| %>
  <% if @ip_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip_name.errors.count, "error") %> prohibited this ip_name from being saved:</h2>

      <ul>
      <% @ip_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :is_valid %><br>
    <%= f.check_box :is_valid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
